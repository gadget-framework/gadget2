#include "intvector.h"

#ifdef GADGET_INLINE
inline
#endif
int& intvector::operator [] (int pos) {
  return v[pos];
}

#ifdef GADGET_INLINE
inline
#endif
const int& intvector::operator [] (int pos) const {
  return v[pos];
}
