#include "agebandmatrixptrvector.h"

#ifdef INLINE_VECTORS
inline
#endif
Agebandmatrixvector*& agebandmatrixptrvector::operator [] (int pos) {
  assert(0 <= pos && pos < size);
  return(v[pos]);
}

#ifdef INLINE_VECTORS
inline
#endif
Agebandmatrixvector* const& agebandmatrixptrvector::operator [] (int pos) const {
  assert(0 <= pos && pos < size);
  return(v[pos]);
}


