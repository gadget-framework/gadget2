#include "addr_keepvector.h"

#ifdef INLINE_VECTORS
inline
#endif
addr_keep& addr_keepvector::operator [] (int pos) {
  return(v[pos]);
}

#ifdef INLINE_VECTORS
inline
#endif
const addr_keep& addr_keepvector::operator [] (int pos) const {
  return(v[pos]);
}
